namespace SprinklingApp.Model.Enums
{
    public enum OperationTypes
    {
        Default,
        PlannedGroupWork, // planned group work item
        SpecifiedGroup, // unplanned group work request
        SpecifiedValve // unplanned valve work request
    }
}
